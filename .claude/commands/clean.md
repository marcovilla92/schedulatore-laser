---
description: Reset project to clean state (kill servers, clear caches)
allowed-tools: Bash
---

Execute the project cleanup automation script:

bash .claude/commands/scripts/clean.sh

This resets the project to a clean state by killing servers and clearing caches.
