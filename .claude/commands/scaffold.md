---
description: Generate boilerplate (components, APIs, tests, utils)
allowed-tools: Bash
argument-hint: <type> <name>
---

Execute the scaffolding automation script:

bash .claude/commands/scripts/scaffold.sh $ARGUMENTS

This generates boilerplate code for components, APIs, tests, and utilities.
