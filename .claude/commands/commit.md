---
description: Smart commit workflow with quality checks and auto-staging
allowed-tools: Bash
argument-hint: [commit-message]
---

Execute the smart commit workflow script:

bash .claude/commands/scripts/commit.sh $ARGUMENTS

This runs quality checks, stages changes, and creates a commit with the provided message.
