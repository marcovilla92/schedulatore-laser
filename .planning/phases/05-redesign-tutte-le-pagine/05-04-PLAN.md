---
phase: 05-redesign-tutte-le-pagine
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/frontend/piega.html
  - app/frontend/saldatura.html
autonomous: true
requirements: [REPT-01, ACCS-01, ACCS-02]

must_haves:
  truths:
    - "piega.html carica design.css e shared.js — nessun Google Fonts ne JetBrains Mono CDN"
    - "saldatura.html carica design.css e shared.js — nessun Google Fonts ne JetBrains Mono CDN"
    - "piega.html ha data-page='piega' per accent ambra; saldatura.html ha data-page='saldatura' per accent arancione"
    - "JetBrains Mono sostituito con 'Courier New', 'Lucida Console', monospace su timer-display e elementi monospace"
    - "I timer attivi per ordine sopravvivono ai cicli di auto-refresh senza resettarsi"
    - "Tutti i bottoni: min-height 48px; Avvia Fase e Completa: min-height 56px"
    - "Il testo principale usa --text-primary con contrasto WCAG AA 4.5:1+"
    - "La barra di ricerca nella navbar filtra ordini per cliente/ID"
    - "I chip filtri per stato (Tutti/In Attesa/In Lavorazione) funzionano con contatori"
    - "Auto-refresh 5s preserva filtri, ricerca e timer attivi"
  artifacts:
    - path: "app/frontend/piega.html"
      provides: "Pagina piega ridisegnata con accent ambra"
      contains: "data-page=\"piega\""
    - path: "app/frontend/saldatura.html"
      provides: "Pagina saldatura ridisegnata con accent arancione"
      contains: "data-page=\"saldatura\""
  key_links:
    - from: "app/frontend/piega.html"
      to: "/design.css"
      via: "link rel=stylesheet"
      pattern: '<link rel="stylesheet" href="/design.css">'
    - from: "app/frontend/saldatura.html"
      to: "/design.css"
      via: "link rel=stylesheet"
      pattern: '<link rel="stylesheet" href="/design.css">'
    - from: "app/frontend/piega.html"
      to: "hasDataChanged"
      via: "fetch/render separation"
      pattern: "hasDataChanged\\(orderList"
    - from: "app/frontend/saldatura.html"
      to: "hasDataChanged"
      via: "fetch/render separation"
      pattern: "hasDataChanged\\(orderList"
---

<objective>
Ridisegnare piega.html e saldatura.html replicando il pattern della reference implementation (laser.html, piano 05-03) con accent theming distinto (ambra per piega, arancione per saldatura), sostituendo JetBrains Mono CDN con font monospace di sistema, preservando i timer attivi per ordine.

Purpose: Applicare il design system v1.2 alle due pagine reparto rimanenti, garantendo identita visiva unica per fase e preservando i timer di lavorazione.
Output: piega.html e saldatura.html completamente ridisegnate.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-redesign-tutte-le-pagine/05-RESEARCH.md
@.planning/phases/04-design-system-condiviso/04-01-SUMMARY.md
@app/frontend/design.css
@app/frontend/shared.js
@app/frontend/piega.html
@app/frontend/saldatura.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ridisegnare piega.html con design system, accent ambra, ricerca, filtri, timer preservati</name>
  <files>app/frontend/piega.html</files>
  <action>
Applicare a piega.html lo stesso pattern di laser.html reference implementation, con accent ambra e preservazione timer.

**1. Head — Sostituire Google Fonts con design.css:**
- Rimuovere le 3 righe `<link>` di Google Fonts (ATTENZIONE: piega.html importa ANCHE JetBrains Mono — rimuovere l'intera riga che include `JetBrains+Mono`)
- Aggiungere `<link rel="stylesheet" href="/design.css">`

**2. Body — Accent theming ambra (REPT-01):**
- Cambiare `<body>` in `<body data-page="piega">`
- Questo attiva `--page-accent: var(--accent-amber)` definito in design.css

**3. CSS — Strippare duplicati, come laser.html:**
- RIMUOVERE: stessi elementi di laser (`:root`, `* reset`, `body`, `body::before`, keyframes, nav, glass-card, btn-primary/secondary, message, loading, spinner, main, scrollbar, @media 768px nav/main)
- MANTENERE page-specific: `.page-header`, `.summary-box`, `.summary-item`, `.orders-grid`, `.order-card`, `.order-header`, `.articles-section`, `.article-item`, `.status-badge` varianti, `.action-buttons`, `.btn-avvia`, `.btn-complete`, `.timer-display`, `.timer-display.active`, `.modal-overlay`, `.modal-content`, `.modal-checkboxes`

**4. JetBrains Mono — Sostituire con font di sistema:**
- Cercare TUTTE le occorrenze di `'JetBrains Mono'` nel CSS e sostituire con `'Courier New', 'Lucida Console', monospace`
- Elementi interessati: `.order-id`, `.article-qty`, `.timer-display` (e varianti)
- NESSUN riferimento a `JetBrains Mono` deve restare nel file

**5. Aggiungere body::before con tinta ambra specifica piega:**
```css
body::before {
  background:
    radial-gradient(circle at 20% 50%, rgba(255, 165, 2, 0.04) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(168, 85, 247, 0.03) 0%, transparent 50%),
    radial-gradient(circle at 50% 80%, rgba(255, 165, 2, 0.02) 0%, transparent 50%);
}
```

**6. Aggiungere CSS nuovi (identici a laser.html ma con --page-accent per colori):**
- Nav search bar (identico a laser)
- Filter chips (identico a laser, usa --page-accent)
- Touch targets: `.btn-avvia, .btn-complete { min-height: 56px; }`, `.btn-modal-cancel, .btn-modal-confirm, .btn-modal-all { min-height: 48px; }`, `nav a { min-height: 40px; }`
- Responsive: `@media (max-width: 1023px) { .orders-grid { grid-template-columns: 1fr; } }` e `@media (min-width: 1024px) { .orders-grid { grid-template-columns: repeat(auto-fill, minmax(340px, 1fr)); } }`

**7. Colori hardcoded ambra — sostituire con var(--page-accent):**
- Dove il CSS usa colori ambra hardcoded (#ffa502, rgba(255,165,2,...)), sostituire con `var(--page-accent)` o `color-mix(in srgb, var(--page-accent) XX%, transparent)` per coerenza design system

**8. HTML — Navbar con ricerca, chip filtri stato:**
- Rimuovere `<a href="welcome.html">Caricamento</a>` dalla nav
- Aggiungere nav-search input (stessa struttura di laser)
- Aggiungere chip filtri stato HTML (Tutti/In Attesa/In Lavorazione) sotto page-header

**9. Script — shared.js + fetch/render separation + timer preservation:**
- Aggiungere `<script src="/shared.js"></script>` PRIMA dello script inline
- Variabili globali: `let allOrders = []; let searchQuery = ''; let statusFilter = 'all'; let lastDataHash = null;`
- Refactorare `loadOrders()` per usare `hasDataChanged()` da shared.js
- Aggiungere `applyFiltersAndRender()` con `filterOrders()` + filtro stato
- Aggiungere chip counts + `setStatusFilter()` + `onSearch()` con `debounce()`
- CRITICO per timer: l'oggetto `timers = {}` (che mappa orderId -> intervalId) DEVE vivere FUORI da `renderOrders()`. Quando `renderOrders()` ricostruisce il DOM, NON deve fermare i timer attivi. Deve solo aggiornare i riferimenti agli elementi DOM dei timer.
  - Pattern: `renderOrders()` costruisce il nuovo HTML, poi per ogni ordine con timer attivo, aggiorna `updateTimerDisplay(orderId)` senza chiamare `clearInterval`
  - I timer si fermano SOLO quando la fase viene completata (callback di `completePhase`)

**10. WCAG contrasto (ACCS-02):**
- Stesse regole di laser: --text-primary per testo principale, --text-secondary per label, --text-muted solo decorativo
  </action>
  <verify>
1. Aprire piega.html — accent ambra visibile su chip, bottoni, hover
2. DevTools Network: NESSUNA richiesta a fonts.googleapis.com ne fonts.gstatic.com
3. Ricerca navbar + chip filtri stato funzionano
4. Se un timer e attivo, attendere 15+ sec — il timer continua senza reset
5. `python -c "content=open('app/frontend/piega.html').read(); assert 'fonts.googleapis.com' not in content; assert 'JetBrains' not in content; assert 'design.css' in content; assert 'shared.js' in content; assert 'data-page=\"piega\"' in content; print('OK')"` deve stampare OK
  </verify>
  <done>
piega.html usa design.css con accent ambra, nessun Google Fonts/JetBrains CDN, fetch/render separation, ricerca + chip filtri stato, timer preservati su auto-refresh, touch target 48/56px, responsive 1024px.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ridisegnare saldatura.html con design system, accent arancione, ricerca, filtri, timer preservati</name>
  <files>app/frontend/saldatura.html</files>
  <action>
Applicare a saldatura.html lo STESSO identico pattern di piega.html (task 1), con le sole differenze:

**1. Accent arancione invece di ambra:**
- `<body data-page="saldatura">` (attiva `--page-accent: var(--accent-orange)` da design.css)
- body::before con tinta arancione:
```css
body::before {
  background:
    radial-gradient(circle at 20% 50%, rgba(255, 99, 72, 0.04) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(168, 85, 247, 0.03) 0%, transparent 50%),
    radial-gradient(circle at 50% 80%, rgba(255, 99, 72, 0.02) 0%, transparent 50%);
}
```

**2. Fase SALDATURA:**
- `const PHASE = 'SALDATURA';` (invece di PIEGA)
- Titolo pagina: `<title>Stazione Saldatura - Schedulatore Laser</title>`

**3. Tutto il resto e IDENTICO a piega.html:**
- Stessa procedura di stripping CSS duplicato
- Stessa sostituzione JetBrains Mono con `'Courier New', 'Lucida Console', monospace`
- Stessa nav search, chip filtri stato, fetch/render separation con shared.js
- Stessa preservazione timer (timers{} fuori da renderOrders)
- Stessi touch target 48/56px
- Stessa responsive 1024px
- Stesse regole WCAG contrasto
- Rimuovere `<a href="welcome.html">Caricamento</a>` dalla nav

**4. Colori hardcoded arancione — sostituire con var(--page-accent):**
- Stessa logica di piega ma per colori arancione (#ff6348, rgba(255,99,72,...))
  </action>
  <verify>
1. Aprire saldatura.html — accent arancione visibile (distinto dall'ambra di piega)
2. DevTools Network: NESSUNA richiesta a fonts.googleapis.com ne fonts.gstatic.com
3. Ricerca navbar + chip filtri stato funzionano
4. Timer attivi sopravvivono a 3+ cicli auto-refresh
5. `python -c "content=open('app/frontend/saldatura.html').read(); assert 'fonts.googleapis.com' not in content; assert 'JetBrains' not in content; assert 'design.css' in content; assert 'shared.js' in content; assert 'data-page=\"saldatura\"' in content; print('OK')"` deve stampare OK
  </verify>
  <done>
saldatura.html usa design.css con accent arancione, nessun Google Fonts/JetBrains CDN, fetch/render separation, ricerca + chip filtri stato, timer preservati su auto-refresh, touch target 48/56px, responsive 1024px. Un operatore distingue immediatamente piega (ambra) da saldatura (arancione).
  </done>
</task>

</tasks>

<verification>
- Nessun riferimento a `fonts.googleapis.com` ne `JetBrains` in entrambi i file
- piega.html: `data-page="piega"` (accent ambra), saldatura.html: `data-page="saldatura"` (accent arancione)
- `<link rel="stylesheet" href="/design.css">` in entrambi
- `<script src="/shared.js"></script>` in entrambi
- Ricerca navbar e chip filtri stato in entrambi
- Timer attivi non si resettano durante auto-refresh in entrambi
- Touch target 48/56px in entrambi
- Font monospace: `'Courier New', 'Lucida Console', monospace` (no JetBrains)
- Responsive 1024px in entrambi
- Testo primario con --text-primary, WCAG AA rispettato
</verification>

<success_criteria>
piega.html e saldatura.html sono ridisegnate con il design system v1.2, ciascuna con accent theming distinto (ambra/arancione) che permette all'operatore di identificare immediatamente il reparto. Pattern identico a laser.html reference implementation: fetch/render, ricerca, chip filtri, timer preservati, WCAG, responsive.
</success_criteria>

<output>
After completion, create `.planning/phases/05-redesign-tutte-le-pagine/05-04-SUMMARY.md`
</output>
