---
phase: 05-redesign-tutte-le-pagine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/frontend/laser.html
autonomous: true
requirements: [REPT-01, REPT-02, REPT-03, FILT-01, FILT-02, FILT-04, ACCS-01, ACCS-02]

must_haves:
  truths:
    - "laser.html carica design.css e shared.js — nessun riferimento a fonts.googleapis.com"
    - "body ha data-page='laser' per attivare l'accento rosso dal design system"
    - "La barra di ricerca nella navbar filtra ordini laser per cliente/ID in tempo reale"
    - "I chip filtro per stato (Tutti/In Attesa/In Lavorazione) funzionano e mostrano contatori"
    - "Auto-refresh 5s aggiorna i dati senza resettare ne la ricerca ne il filtro per stato"
    - "Dopo 3+ cicli di auto-refresh il filtro selezionato resta attivo"
    - "Tutti i bottoni: min-height 48px; Avvia Fase e Completa: min-height 56px"
    - "Layout: >=1024px multi-colonna (cards grid), <1024px colonna singola"
    - "Il testo principale usa --text-primary con contrasto WCAG AA 4.5:1+"
  artifacts:
    - path: "app/frontend/laser.html"
      provides: "Pagina laser ridisegnata — implementazione di riferimento per piega/saldatura"
      contains: "data-page=\"laser\""
  key_links:
    - from: "app/frontend/laser.html"
      to: "/design.css"
      via: "link rel=stylesheet"
      pattern: '<link rel="stylesheet" href="/design.css">'
    - from: "app/frontend/laser.html"
      to: "/shared.js"
      via: "script src"
      pattern: '<script src="/shared.js">'
    - from: "app/frontend/laser.html"
      to: "hasDataChanged"
      via: "fetch/render separation using shared.js"
      pattern: "hasDataChanged\\(orderList"
    - from: "app/frontend/laser.html"
      to: "filterOrders"
      via: "search filtering from shared.js"
      pattern: "filterOrders\\(allOrders"
---

<objective>
Ridisegnare laser.html come implementazione di riferimento del design system v1.2: accent theming rosso via body[data-page], separazione fetch/render con shared.js per preservare filtri durante auto-refresh 5s, ricerca globale nella navbar, chip filtri per stato (in_attesa/in_lavorazione), touch target WCAG 48/56px, responsive 1024px.

Purpose: Creare il pattern canonico che piega.html e saldatura.html replicheranno nel piano 05-04.
Output: laser.html completamente ridisegnata come reference implementation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-redesign-tutte-le-pagine/05-RESEARCH.md
@.planning/phases/04-design-system-condiviso/04-01-SUMMARY.md
@app/frontend/design.css
@app/frontend/shared.js
@app/frontend/laser.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrare design system, accent theming e strippare CSS duplicato da laser.html</name>
  <files>app/frontend/laser.html</files>
  <action>
Modificare head, CSS inline e body di laser.html per integrare il design system condiviso con accent rosso.

**1. Head — Sostituire Google Fonts con design.css:**
- Rimuovere le 3 righe `<link>` di Google Fonts (preconnect + stylesheet)
- Aggiungere `<link rel="stylesheet" href="/design.css">` come primo link nel `<head>`

**2. Body — Aggiungere data-page per accent theming (REPT-01):**
- Cambiare `<body>` in `<body data-page="laser">`
- Questo attiva automaticamente `--page-accent: var(--accent-red)` definito in design.css @layer tokens

**3. Inline CSS — Strippare duplicati, tenere solo page-specific:**
- RIMUOVERE: `:root` (tutti i token), `* reset`, `body { font-family, background, color, min-height, overflow-x }`, `body::before` ambient animation (design.css base layer lo include gia), `@keyframes ambientFloat/fadeInUp/spin`, `nav` + `.nav-logo` + `nav a`, `.glass-card`, `.btn-primary`/`.btn-secondary`, `.message`/`.loading`/`.spinner`, `main`, `::-webkit-scrollbar`, `@media (max-width: 768px)` per nav/main
- MANTENERE (page-specific che NON sono in design.css):
  `.page-header`, `.summary-box`, `.summary-item`, `.summary-number`, `.summary-label`,
  `.orders-grid`, `.order-card`, `.order-header`, `.order-title`, `.order-info`, `.order-id`,
  `.articles-section`, `.articles-header`, `.article-item`, `.article-name`, `.article-qty`,
  `.status-badge`, `.status-ready`, `.status-started`, `.status-completed`,
  `.action-buttons`, `.btn-avvia`, `.btn-complete`,
  `.modal-overlay`, `.modal-content`, `.modal-header`, `.modal-checkboxes`, `.modal-checkbox-item`, `.btn-modal-cancel`, `.btn-modal-confirm`, `.btn-modal-all`

**4. Aggiungere CSS NUOVI per body::before con tinta rossa specifica laser:**
```css
body::before {
  background:
    radial-gradient(circle at 20% 50%, rgba(255, 71, 87, 0.04) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(168, 85, 247, 0.03) 0%, transparent 50%),
    radial-gradient(circle at 50% 80%, rgba(255, 71, 87, 0.02) 0%, transparent 50%);
}
```

**5. Aggiungere CSS page-specific NUOVI:**

Nav search bar:
```css
.nav-search { margin-left: auto; display: flex; align-items: center; }
.nav-search input {
  background: var(--bg-input); border: 1px solid var(--border-glass);
  border-radius: var(--radius-sm); color: var(--text-primary);
  font-size: 13px; padding: 7px 14px; width: 200px; height: 36px;
  transition: var(--transition);
}
.nav-search input:focus {
  outline: none; border-color: color-mix(in srgb, var(--page-accent) 40%, transparent);
  background: rgba(255, 255, 255, 0.08); width: 260px;
}
.nav-search input::placeholder { color: var(--text-muted); }
```

Filter chips per stato (FILT-02 + FILT-03):
```css
.filter-chips { display: flex; gap: 8px; margin-bottom: 20px; flex-wrap: wrap; }
.chip {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 8px 16px; border-radius: 20px;
  border: 1px solid var(--border-glass);
  background: rgba(255, 255, 255, 0.04);
  color: var(--text-secondary); font-size: 13px; font-weight: 600;
  cursor: pointer; transition: var(--transition); min-height: 36px;
}
.chip.active {
  background: color-mix(in srgb, var(--page-accent) 15%, transparent);
  border-color: color-mix(in srgb, var(--page-accent) 40%, transparent);
  color: var(--page-accent);
}
.chip-count {
  background: rgba(255, 255, 255, 0.1); border-radius: 10px;
  padding: 1px 6px; font-size: 11px;
}
.chip.active .chip-count {
  background: color-mix(in srgb, var(--page-accent) 20%, transparent);
}
```

Touch targets (ACCS-01):
```css
nav a { min-height: 40px; display: flex; align-items: center; }
.btn-avvia, .btn-complete { min-height: 56px; padding: 14px 20px; display: inline-flex; align-items: center; justify-content: center; }
.btn-modal-cancel, .btn-modal-confirm, .btn-modal-all { min-height: 48px; padding: 12px 20px; }
```

Responsive 1024px (REPT-03):
```css
@media (max-width: 1023px) {
  .orders-grid { grid-template-columns: 1fr; }
  .summary-box { grid-template-columns: repeat(2, 1fr); }
}
@media (min-width: 1024px) {
  .orders-grid { grid-template-columns: repeat(auto-fill, minmax(340px, 1fr)); }
}
```

**6. Sostituire colori hardcoded con variabili page-accent:**
Nei componenti page-specific, dove prima c'erano colori rossi hardcoded (es. gradients su .btn-avvia, border-color su .order-card), usare `var(--page-accent)` o `color-mix(in srgb, var(--page-accent) ...)` per coerenza con il design system. Questo rende il pattern facilmente replicabile per piega/saldatura cambiando solo `data-page`.

**7. WCAG contrasto (ACCS-02):**
- Testo primario (nomi clienti, articoli, numeri ordine): `color: var(--text-primary)`
- Label secondarie, metadata, contatori: `color: var(--text-secondary)`
- ID abbreviati, separatori decorativi: `color: var(--text-muted)`
  </action>
  <verify>
1. Aprire laser.html — pagina carica senza errori console
2. DevTools Network: nessuna richiesta a fonts.googleapis.com
3. Verificare che l'accento rosso e visibile (bottoni, chip attivo, hover)
4. `python -c "content=open('app/frontend/laser.html').read(); assert 'fonts.googleapis.com' not in content; assert 'design.css' in content; assert 'data-page=\"laser\"' in content; print('CSS OK')"` deve stampare "CSS OK"
  </verify>
  <done>
laser.html usa design.css con accent theming rosso via data-page="laser". CSS inline ridotto a sole regole page-specific. Touch target 48/56px. Responsive 1024px.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementare fetch/render separation, ricerca navbar, e chip filtri stato in laser.html</name>
  <files>app/frontend/laser.html</files>
  <action>
Refactorare il JavaScript di laser.html per implementare la separazione fetch/render completa usando shared.js, e aggiungere ricerca + chip filtri per stato.

**1. HTML navbar — Aggiungere ricerca e aggiornare link:**
- Rimuovere il link `<a href="welcome.html">Caricamento</a>` dalla nav
- Aggiungere prima della chiusura `</nav>`:
```html
<div class="nav-search">
  <input type="search" id="navSearch" placeholder="Cerca cliente..." autocomplete="off" oninput="onSearch(this.value)">
</div>
```

**2. HTML — Aggiungere chip filtri per stato (FILT-02) sotto page-header, prima di orders-grid:**
```html
<div class="filter-chips">
  <button class="chip active" onclick="setStatusFilter('all')" id="chip-all">
    Tutti <span class="chip-count" id="count-all">0</span>
  </button>
  <button class="chip" onclick="setStatusFilter('in_attesa')" id="chip-attesa">
    In Attesa <span class="chip-count" id="count-attesa">0</span>
  </button>
  <button class="chip" onclick="setStatusFilter('in_lavorazione')" id="chip-lavorazione">
    In Lavorazione <span class="chip-count" id="count-lavorazione">0</span>
  </button>
</div>
```

**3. Script — Aggiungere shared.js e refactorare completamente il fetch/render (REPT-02):**
- Aggiungere `<script src="/shared.js"></script>` PRIMA dello `<script>` inline

Variabili globali:
```javascript
const API_URL = '/api';
const PHASE = 'LASER';
let allOrders = [];
let searchQuery = '';
let statusFilter = 'all';
let lastDataHash = null;
```

Funzione loadOrders refactored per usare shared.js `hasDataChanged()`:
```javascript
async function loadOrders(forceRender) {
  try {
    const response = await fetch(`${API_URL}/phase/${PHASE}/orders`);
    const orders = await response.json();
    const orderList = Array.isArray(orders) ? orders : [];

    const { changed, newHash } = hasDataChanged(orderList, lastDataHash);
    if (!forceRender && !changed) return;
    lastDataHash = newHash;

    allOrders = orderList;
    applyFiltersAndRender();
  } catch (error) {
    showMessage('Errore connessione al server', 'error');
  }
}
```

Funzione `applyFiltersAndRender()` che applica ENTRAMBI i filtri (ricerca + stato):
```javascript
function applyFiltersAndRender() {
  let filtered = filterOrders(allOrders, searchQuery);
  if (statusFilter !== 'all') {
    filtered = filtered.filter(order =>
      (order.articles_in_phase || []).some(a => a.phase_status === statusFilter)
    );
  }
  updateChipCounts();
  renderOrders(filtered);
  updateSummary(filtered);
}
```

Chip counts (FILT-02):
```javascript
function updateChipCounts() {
  const total = filterOrders(allOrders, searchQuery);
  const attesa = total.filter(o => (o.articles_in_phase || []).some(a => a.phase_status === 'in_attesa'));
  const lavorazione = total.filter(o => (o.articles_in_phase || []).some(a => a.phase_status === 'in_lavorazione'));
  document.getElementById('count-all').textContent = total.length;
  document.getElementById('count-attesa').textContent = attesa.length;
  document.getElementById('count-lavorazione').textContent = lavorazione.length;
}

function setStatusFilter(status) {
  statusFilter = status;
  document.querySelectorAll('.chip').forEach(c => c.classList.remove('active'));
  document.getElementById('chip-' + (status === 'all' ? 'all' : status === 'in_attesa' ? 'attesa' : 'lavorazione')).classList.add('active');
  applyFiltersAndRender();
}
```

Search handler usando shared.js `debounce()` e `filterOrders()`:
```javascript
const debouncedSearch = debounce((q) => {
  searchQuery = q;
  applyFiltersAndRender();
}, 300);

function onSearch(value) {
  debouncedSearch(value);
}
```

DOMContentLoaded:
```javascript
window.addEventListener('DOMContentLoaded', () => {
  loadOrders(true);
  setInterval(() => loadOrders(), 5000);
});
```

**4. Preservare tutta la logica esistente:**
- La funzione `renderOrders(orders)` deve continuare a generare le order-card con articoli, status badge, bottoni avvia/completa
- Le funzioni `startPhase()`, `openCompletionModal()`, `confirmCompletion()`, `showMessage()` restano invariate
- Il modal per selezione articoli da completare resta invariato
- SOLO il flusso di caricamento dati cambia: prima era `loadOrders() -> renderOrders(orders)` diretto, ora e `loadOrders() -> allOrders = data -> applyFiltersAndRender() -> renderOrders(filtered)`

**5. Verificare preservazione filtri su auto-refresh (FILT-04):**
- L'auto-refresh a 5s chiama `loadOrders()` che aggiorna `allOrders` e richiama `applyFiltersAndRender()` con il `searchQuery` e `statusFilter` correnti — NON li resetta
- Se l'utente ha selezionato "In Attesa" e digitato "Rossi" nella ricerca, dopo 3+ cicli di auto-refresh quei filtri devono essere ancora attivi
  </action>
  <verify>
1. Aprire laser.html con server attivo — verificare ricerca nella navbar e chip filtri
2. Selezionare chip "In Attesa" — verificare che i chip mostrano i contatori corretti
3. Attendere 15+ secondi (3 cicli auto-refresh) — verificare che il chip "In Attesa" resta selezionato e la ricerca non si resetta
4. Digitare un nome cliente nella ricerca — verificare che gli ordini si filtrano e i chip aggiornano i contatori
5. Click "Avvia Fase" su un ordine — verificare che la funzionalita resta invariata
6. `python -c "content=open('app/frontend/laser.html').read(); assert 'shared.js' in content; assert 'hasDataChanged' in content; assert 'filterOrders' in content; assert 'statusFilter' in content; assert 'applyFiltersAndRender' in content; print('OK')"` deve stampare OK
  </verify>
  <done>
laser.html ha fetch/render separation completa con shared.js: hasDataChanged() evita re-render inutili, filterOrders() per ricerca, chip filtri per stato con contatori. Auto-refresh 5s preserva filtri e ricerca. Questa e l'implementazione di riferimento per piega/saldatura (piano 05-04).
  </done>
</task>

</tasks>

<verification>
- Nessun riferimento a `fonts.googleapis.com`
- `<link rel="stylesheet" href="/design.css">` nel head
- `<body data-page="laser">` per accent rosso
- `<script src="/shared.js"></script>` prima dello script inline
- Ricerca navbar filtra per cliente/ID
- Chip filtri stato: Tutti/In Attesa/In Lavorazione con contatori
- Auto-refresh 5s non resetta ne ricerca ne chip selezionato
- hasDataChanged() da shared.js per evitare re-render
- Bottoni Avvia Fase/Completa: min-height 56px
- Bottoni secondari: min-height 48px
- Layout multi-colonna >=1024px, colonna singola <1024px
- Testo primario con --text-primary, accent con --page-accent
- Tutte le funzionalita esistenti (avvia/completa fase, modal articoli) preservate
</verification>

<success_criteria>
laser.html e la reference implementation del design system v1.2: accent theming rosso, fetch/render separation con shared.js, ricerca + chip filtri per stato che sopravvivono a 3+ cicli auto-refresh, touch target WCAG 48/56px, responsive 1024px. Questo pattern sara replicato in piega.html e saldatura.html dal piano 05-04.
</success_criteria>

<output>
After completion, create `.planning/phases/05-redesign-tutte-le-pagine/05-03-SUMMARY.md`
</output>
