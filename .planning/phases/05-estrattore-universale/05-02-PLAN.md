---
phase: 05-estrattore-universale
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - app/test_universal_extractor.py
autonomous: false
requirements:
  - EXTR-04

must_haves:
  truths:
    - "Eseguendo lo script di test sui PDF di audit, si ottiene un confronto per-formato tra baseline Phase 4 e risultati universal_extractor"
    - "Il success rate complessivo del universal_extractor supera la baseline sui formati con punteggio basso (ORDINE_LS cliente 50%, data 50%)"
    - "L'utente puo ispezionare il testo Docling intermedio via flag --debug per debuggare estrazioni problematiche"
    - "Il modulo e eseguibile singolarmente su un PDF specifico dalla CLI e produce JSON leggibile"
  artifacts:
    - path: "app/test_universal_extractor.py"
      provides: "Script di validazione e confronto baseline"
      min_lines: 100
  key_links:
    - from: "app/test_universal_extractor.py"
      to: "app/backend/universal_extractor.py"
      via: "import extract_universal"
      pattern: "extract_universal"
    - from: "app/test_universal_extractor.py"
      to: "app/audit_reports/"
      via: "confronto con baseline Phase 4"
      pattern: "baseline|audit"
---

<objective>
Validare il modulo universal_extractor sui PDF reali dell'audit (Phase 4) e verificare
che il success rate superi la baseline per i formati con punteggio basso.

Purpose: Dimostrare che l'estrattore universale risolve i casi fallimentari identificati
in Phase 4 (ORDINE_LS cliente 50%, data_consegna 50%; FOR_ORDINE data 80%).

Output:
- app/test_universal_extractor.py (script di validazione con confronto baseline)
- Checkpoint di verifica: utente legge il report e conferma che i target sono raggiunti
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-estrattore-universale/05-CONTEXT.md
@.planning/phases/05-estrattore-universale/05-RESEARCH.md
@.planning/phases/05-estrattore-universale/05-01-SUMMARY.md
@app/audit_parsers.py
@app/audit_reports/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creare script di validazione test_universal_extractor.py</name>
  <files>app/test_universal_extractor.py</files>
  <action>
Creare app/test_universal_extractor.py — script standalone che:

1. Carica l'API key da app/.env tramite load_dotenv(Path(__file__).parent / ".env")
2. Usa la stessa directory PDF dell'audit di Phase 4: C:\Users\39334\Documents\ORDINI
   (accettare anche --dir come argomento CLI opzionale)
3. Per ogni PDF nella directory, chiama extract_universal(filepath) e raccoglie:
   - cliente (non vuoto = PASS)
   - numero_ordine (non vuoto = PASS)
   - data_consegna (non uguale a datetime.now().isoformat() = PASS, ovvero non il default fallback)
   - articoli (lista non vuota = PASS)
   - I corrispondenti campi _confidence
4. Stampa un report comparativo con la baseline Phase 4 (vedere struttura in how-to-verify)
5. Gestisce ExtractionError per singolo PDF: stampa l'errore e continua (non interrompe il batch)
6. Alla fine stampa un verdetto: SUCCESS oppure ATTENZIONE con i campi che non superano la baseline

Struttura del file:
- Shebang #!/usr/bin/env python3
- Docstring con uso
- Fix encoding Windows: sys.stdout.reconfigure(encoding='utf-8', errors='replace')
- sys.path.insert(0, str(Path(__file__).parent)) per import backend
- Import di extract_universal, ExtractionError da backend.universal_extractor
- Baseline Phase 4 hardcoded:
    BASELINE_PHASE4 = {
        "cliente": 0.87,
        "numero_ordine": 1.00,
        "data_consegna": 0.81,
        "articoli": 1.00,
    }
- Deduplicazione PDF: usa set() sui filename senza directory (la cartella ha 32 file = 16 x 2 duplicati)
- Rilevamento data_consegna fallback: considerare FALLBACK se il valore inizia con datetime.now().strftime('%Y-%m-%d')

Il report deve includere:
- Sezione RISULTATI PER CAMPO: tabella con Phase 4 Baseline, Universal Extractor, Delta
- Sezione RISULTATI PER FILE: una riga per PDF con stato e confidence per ogni campo
- Sezione ANALISI CONFIDENCE: lista dei campi estratti con confidence bassa
- Sezione CONFRONTO BASELINE: verdetto esplicito su ORDINE_LS e overall

Eseguire syntax check dopo creazione:
    python -m py_compile app/test_universal_extractor.py
  </action>
  <verify>
    cd app
    python -m py_compile test_universal_extractor.py && echo "SYNTAX OK"
Deve stampare SYNTAX OK senza errori.
  </verify>
  <done>
python -m py_compile test_universal_extractor.py passa senza errori.
Il file contiene BASELINE_PHASE4 con i valori di Phase 4.
Il file accetta --dir come argomento CLI.
Il file gestisce ExtractionError per singolo PDF senza interrompere il batch.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifica validazione universal_extractor vs baseline Phase 4</name>
  <action>Eseguire test_universal_extractor.py su C:\Users\39334\Documents\ORDINI e verificare che il report mostri success rate >= baseline Phase 4 e miglioramento su ORDINE_LS.</action>
  <verify>
    cd app
    python test_universal_extractor.py

Attendersi 1-3 minuti (16 PDF x ~5 secondi ciascuno Docling + Gemini).
Il report deve mostrare la tabella RISULTATI PER CAMPO con valori per Universal Extractor.
  </verify>
  <done>Report generato senza crash; success rate cliente >= 87% e data_consegna >= 81%; ORDINE_LS migliora rispetto al 50%; utente approva.</done>
  <what-built>
Script app/test_universal_extractor.py che esegue universal_extractor su tutti i PDF
di audit e confronta i risultati con la baseline Phase 4 (cliente 87%, data 81%).

Prima di eseguire lo script, assicurarsi che GEMINI_API_KEY sia configurata in app/.env.

Test singolo PDF (utile per debuggare prima del batch completo):
    cd app
    python backend/universal_extractor.py "C:\Users\39334\Documents\ORDINI\<nomefile>.pdf"

Test singolo PDF con testo Docling visibile:
    python backend/universal_extractor.py "C:\Users\39334\Documents\ORDINI\<nomefile>.pdf" --debug

Validazione completa:
    python test_universal_extractor.py
  </what-built>
  <how-to-verify>
1. Verificare che app/.env contenga GEMINI_API_KEY=chiave

2. Eseguire il test su un singolo PDF per primo:
       cd app
       python backend/universal_extractor.py "C:\Users\39334\Documents\ORDINI\<primo_pdf>.pdf"
   Controllare che l'output JSON contenga: cliente, numero_ordine, data_consegna, articoli,
   e i campi _confidence con valori "alta"/"media"/"bassa".

3. Eseguire la validazione completa:
       cd app
       python test_universal_extractor.py
   Attendersi 1-3 minuti (16 PDF x ~5 secondi ciascuno Docling + Gemini).

4. Verificare nel report stampato su stdout:
   - Sezione RISULTATI PER CAMPO: Universal Extractor cliente >= 87%, data_consegna >= 81%
   - Sezione RISULTATI PER FILE: tutti i PDF processati (ExtractionError gestiti, non crash)
   - Sezione CONFRONTO BASELINE: ORDINE_LS migliorato rispetto al 50%
   - Verdetto finale: SUCCESS (o ATTENZIONE con spiegazione)

5. Opzionale — flag --debug per ispezionare testo Docling intermedio:
       python backend/universal_extractor.py "C:\Users\39334\Documents\ORDINI\<pdf>.pdf" --debug
   Il testo Docling deve apparire su stderr prima del JSON su stdout.
  </how-to-verify>
  <resume-signal>Digitare "approvato" se il report mostra success rate >= baseline su tutti i campi, oppure descrivere i problemi riscontrati (es. "ORDINE_LS cliente ancora al 50%", "ExtractionError su 3 PDF") per procedere con eventuali correzioni.</resume-signal>
</task>

</tasks>

<verification>
Criteri di verifica dell'intera Phase 5:

1. app/backend/universal_extractor.py esiste ed e importabile
2. extract_universal(pdf_path) restituisce dict con tutti i campi richiesti
3. I campi _confidence contengono valori "alta"/"media"/"bassa" (non numeri)
4. ExtractionError sollevata su GEMINI_API_KEY mancante (non KeyError)
5. ExtractionError sollevata su risposta Gemini malformata/vuota
6. Testo Docling ispezionabile via --debug flag
7. Success rate universal_extractor >= baseline Phase 4 (87% cliente, 81% data_consegna)
8. ORDINE_LS migliora rispetto a baseline 50%
</verification>

<success_criteria>
- Lo script di validazione esegue senza crash su tutti i 16 PDF (32 con duplicati, deduplicati)
- Il report mostra success rate per cliente >= 87% e data_consegna >= 81%
- Almeno un PDF ORDINE_LS viene estratto correttamente (cliente e data trovati)
- L'utente conferma i risultati nel checkpoint e digita "approvato"
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/05-estrattore-universale/05-02-SUMMARY.md`
con: risultati della validazione, success rate per campo, confronto con baseline Phase 4,
eventuali note su PDF problematici o ExtractionError.
</output>
